The purpose of this application:
- to learn full stack development (mainly backend)
- to make a clean apple reminders clone application

The stack is:
- Typescript, NextJS, tailwind
- Drizzle ORM, SQLite



Some things to note,
there are fundamental components that are used in apple reminders, and there are four main "filters" that are applied to these lists.

Today "filter"
- checks the lists and sees which items are due today and only displays them

All "filter"
- checks the items in all lists and displays them

Completed "filter"
- shows completed items

Scheduled "filter"
- shows any future items 



so the first fundamental component is called a 

list:
- this has a name/color/icon
- and this stores the next component called items

item:
- this is the actual to do item that the user can create
- it should store text, date, and a simple integer tracker to check completion (0 or 1)


keep the UI super minimal and basic.

I have provided a skeleton of a note taking application which is very overkill in the amount of components there are compared to what we are trying to do

fonts that I want to use:

inter
space grotesk
halo grotesk
SF mono

Text dark mode for numbers:
#646464
#a1a1a1

Colors:
#b64400
#9659b9
#ee98b7
#0069cc
#50aef6
#abcdef
#169b40
#0a461d
#fcd609
#fc9601
#fc3e2f

app/(main) is our main components

app/(marketing) is our landing and onboarding page for new users

app/(public) can be ignored for now and used later for shareable lists


next things to do for this app 7/4/26
- implement cleaner ui and interaction
    - implement dark mode
    - description for reminder (similar to how you implemented the due date functionality)
    - make it so that a user just has to double click to add a reminder, and getting rid of the add a reminder input at the bottom.
    - make it so that users can move around the order of reminders in a list, regardless of due date
    - reminder rename on demand
    - drag and drop in the sidebar, so a user can drag a reminder into the sidebar and move it to another list if they want too, or they can
      even soft delete the reminder by dragging it on top of the recently deleted folder
    - make an interaction for completing a reminder, where the bubble in the reminder just fills up and the color of the text becomes dark gray to show
      irrelevancy
    - fix search and its icon (low priority)
    - implement an oauth